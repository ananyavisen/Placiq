# Placiq Roadmap

Roadmap page using the exact `bg.png` background from the Placiq mock-interview project.

## Run
```bash
npm install
npm run dev
```

The background is in `src/assets/bg.png` and is applied to the whole page in `App.jsx`.
